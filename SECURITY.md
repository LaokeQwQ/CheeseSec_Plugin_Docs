# 文档与发布安全策略

手册中的命令和配置可能影响生产 WAF。所有操作说明必须给出前置条件、最小权限、确认提示、审计字段、失败处理和回滚路径。

官方与企业普通发布至少 2-of-3 签名，高风险发布 3-of-5；企业使用独立信任根。社区、个人、测试、开发、未知和不可信来源必须明确风险，不能默认获得高权限。风险操作要先显示告警并等待 10 秒，再完成密码、二次和三次确认。

禁止在文档、示例和截图中出现私钥、wrapping key、Token、密码、恢复凭证或客户数据。签名根、吊销清单和审计记录应通过受控系统保存；密钥轮换、吊销和异常发布都要有可追溯记录。

离线集群应在不主动连接外网的情况下完成验证和运行。临时联网必须由管理员输入密码确认，并记录连接信息。CWEDP 负责分发和断点续传，传输完成后校验 MD5、SHA-1、SHA-256；校验失败不得激活。

发现文档与实现不一致时，先标为契约或已知限制，暂停“已支持”表述，并补充修复或验收证据。
