# Version-pinned only for local handbook checks. These packages are not
# runtime dependencies and are never included in a CRP artifact. The Linux CI
# runner additionally uses requirements-ci-linux.txt with wheel hashes.
attrs==26.1.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
referencing==0.35.1
rpds-py==0.30.0
